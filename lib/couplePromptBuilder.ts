import { CoupleReportRequestBody, CouplePerson } from "./types";

function levelOf(v: number): string {
  if (v <= 10) return "매우 낮음";
  if (v <= 30) return "낮음";
  if (v <= 70) return "보통";
  if (v <= 90) return "높음";
  return "매우 높음";
}

function formatPerson(p: CouplePerson): string {
  const t = p.temperament;
  const c = p.character;
  const lines = [
    `NS(자극추구)=${t.NS ?? 50}(${levelOf(t.NS ?? 50)})`,
    `HA(위험회피)=${t.HA ?? 50}(${levelOf(t.HA ?? 50)})`,
    `RD(사회적민감성)=${t.RD ?? 50}(${levelOf(t.RD ?? 50)})`,
    `P(인내력)=${t.P ?? 50}(${levelOf(t.P ?? 50)})`,
    `SD(자율성)=${c.SD ?? 50}(${levelOf(c.SD ?? 50)})`,
    `CO(연대감)=${c.CO ?? 50}(${levelOf(c.CO ?? 50)})`,
    `ST(자기초월)=${c.ST ?? 50}(${levelOf(c.ST ?? 50)})`,
  ];
  return `${p.name || "미상"}(${p.gender || "미상"}): ${lines.join(", ")}`;
}

export function buildCouplePrompt(body: CoupleReportRequestBody): string {
  const { person1, person2 } = body;

  return `당신은 커플/부부 상담에 특화된 임상심리전문가입니다. 아래 두 사람의 TCI(기질 및 성격검사) 결과를 바탕으로 "커플 TCI 기질 및 성격검사 해석상담보고서"를 작성하십시오.

## 검사 결과 (백분위점수, 50=평균)
- 피검자1 ${formatPerson(person1)}
- 피검자2 ${formatPerson(person2)}

## 작성 지침

1) **동물 비유 부여**: 두 사람 각각에게 실제 프로파일에 어울리는 동물을 하나씩 골라 비유하십시오. 반드시 그 사람의 실제 점수 패턴(자극추구, 위험회피, 사회적 민감성, 인내력, 자율성, 연대감, 자기초월의 조합)에 근거해서 자유롭게 선택하고, 두 사람이 서로 다른 동물이어야 합니다. (예: 조심성과 공감이 높으면 사슴/토끼류, 호기심은 많지만 경계심도 있으면 고양이류, 자극추구가 높고 위험회피가 낮으면 강아지/치타류 등 — 이는 예시일 뿐이며 실제 프로파일에 맞게 자유롭게 선택하십시오.)

2) **모든 서술에 동물 비유를 자연스럽게 녹여 사용**하되, 과도하게 유치해지지 않고 전문적이고 따뜻한 어조를 유지하십시오.

3) **실제 점수를 근거로 구체적으로 서술**하고, 추측성 내용은 조심스러운 표현으로 제시하십시오.

4) **갈등 시나리오**는 두 사람의 실제 기질/성격 차이(특히 차이가 큰 척도)에서 비롯될법한 현실적이고 구체적인 장면으로 2개 작성하십시오. 각 시나리오는 실제 있을 법한 대화나 상황 묘사를 포함해 생생하게 작성하십시오.

5) **맞춤형 처방전**은 두 사람 각각에게 실행 가능한 구체적 행동 지침을 담되, 동물 비유를 활용해 이해하기 쉽게 설명하십시오. (doc 예시처럼 "하루 10분 ○○하기"처럼 실천 가능한 형태로)

6) **종합 제언**에서는 관계의 강점, 주의가 필요한 영역, 구체적 실천 제언(4~5개, 각각 상세하게), 그리고 상담자 코멘트를 포함하십시오. 상담자 코멘트에서는 두 사람의 차이가 옳고 그름이나 우열의 문제가 아님을 분명히 하고, 필요시 정기적인 커플상담을 권장하며 전문적이고 따뜻한 어조로 마무리하십시오.

7) 모든 텍스트는 한국어로, 실제 인쇄될 보고서에 들어갈 완성된 문장으로 작성하십시오.

## 출력 형식
다른 설명이나 마크다운 없이, 아래 JSON 스키마와 정확히 동일한 키를 가진 순수 JSON 객체만 반환하십시오.

{
  "person1_animal": "피검자1에게 부여한 동물 비유 타이틀 (예: 안전한 숲을 좋아하는 다정다감한 사슴, 20자 내외)",
  "person2_animal": "피검자2에게 부여한 동물 비유 타이틀 (person1과 다른 동물)",
  "person1_summary": "피검자1의 기질(타고난 본성)과 성격(가꿔온 내면) 요약, 동물 비유를 녹여서 서술 (400자 내외)",
  "person2_summary": "피검자2의 기질과 성격 요약 (400자 내외)",
  "person1_strength": "피검자1의 강점 (150자 내외)",
  "person1_weakness": "피검자1의 약점 및 특이지표 (150자 내외)",
  "person2_strength": "피검자2의 강점 (150자 내외)",
  "person2_weakness": "피검자2의 약점 및 특이지표 (150자 내외)",
  "person1_animal_desc": "피검자1의 동물 유형에 대한 생동감 있는 설명문, 실제 그 동물의 습성에 빗대어 (150자 내외)",
  "person2_animal_desc": "피검자2의 동물 유형에 대한 설명문 (150자 내외)",
  "temperament_analysis": "두 사람의 기질(NS/HA/RD/P) 척도별 비교 분석, 척도별로 차이와 공통점을 짚어가며 서술 (600자 내외)",
  "character_analysis": "두 사람의 성격(SD/CO/ST) 척도별 비교 분석 (500자 내외)",
  "conflict_scenarios": [
    {"title": "갈등 상황 제목", "story": "구체적인 갈등 시나리오 (300자 내외)"},
    {"title": "갈등 상황 제목", "story": "구체적인 갈등 시나리오 (300자 내외)"}
  ],
  "prescriptions": [
    {"forName": "피검자1 이름", "title": "처방 제목", "detail": "구체적 실천 지침, 동물 비유 활용 (300자 내외)"},
    {"forName": "피검자2 이름", "title": "처방 제목", "detail": "구체적 실천 지침, 동물 비유 활용 (300자 내외)"}
  ],
  "strengths": "두 분 관계의 강점 (250자 내외)",
  "cautions": "주의가 필요한 영역 (300자 내외)",
  "action_items": ["구체적 실천 제언 1 (150자 내외)", "구체적 실천 제언 2", "구체적 실천 제언 3", "구체적 실천 제언 4"],
  "counselor_comment": "상담자 코멘트 (300자 내외)"
}`;
}
